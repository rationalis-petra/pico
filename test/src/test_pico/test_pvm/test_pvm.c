#include "test_pvm/test_pvm.h"
#include "components/pvm/pvm.h"
//#include "test_pvm/helper.h"

void run_pvm_tests(TestLog *log, Allocator *a) {
    //PassSet set = (PassSet){};
    //PVMContext* ctx = mk_pvm_context(set, a);
}
